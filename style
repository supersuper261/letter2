#envelope-container {
  position: relative;
  width: 200px;
  height: 150px;
}

#envelope {
  position: absolute;
  width: 100%;
  height: 100%;
  fill: #000;
}

#flower {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 50px;
  height: 50px;
  fill: #f00;
}

@keyframes openEnvelope {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

#envelope.opening {
  animation: openEnvelope 2s linear forwards;
}
